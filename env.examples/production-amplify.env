# AWS Amplify prod branch (main / master / production) — NOT staging
# amplify.yml sets this for every branch except "staging"
NEXT_PUBLIC_API_BASE_URL=https://pmsapi.customfurnish.com/api/auth
# Do not set API_PROXY_TARGET on prod (no test API proxy)
